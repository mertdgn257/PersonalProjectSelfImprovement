import { KeyTakeaway } from '../KeyTakeaway';

export default function KeyTakeawayExample() {
  return (
    <KeyTakeaway text="Small, consistent changes compound into meaningful personal development over time." />
  );
}
